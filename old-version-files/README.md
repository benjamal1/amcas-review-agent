# old-version-files

Pre-rebuild Obsidian vault structure, kept for reference. Superseded by `content/`
(`data.json` + `documents/`) per phase 06 and folded into the data model in phase 07.
Not read by the app. Safe to delete once nothing references it.
