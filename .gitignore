# Obsidian internal files
.obsidian/
.trash/

# Smart Connections plugin data
.smart-env/

# OS files
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Node (if any tooling added later)
node_modules/

# Personal data (should already be cleared in this repo, but just in case)
# Meeting Notes/*.md is tracked (contains only the README placeholder)

# content/ ships tracked with demo data (see README) — real personal data belongs
# in a separate directory pointed to via CONTENT_DIR / the Settings page, not here.
# This override file backs that Settings page; never commit it.
.local-config.json
# Example real-data dir name if kept inside the repo (see README "Getting started").
/BJ_content/

# Generated score files
*-scores.md
applicant-image.md
session-context-*.md

# Agent workspace
.agents/

# Build output
dist/

# Local overrides
*.local

# Playwright MCP screenshot/snapshot artifacts
.playwright-mcp/
/*.png
dist-static/

# Internal dev-process docs (not part of the shipped app)
/.planning/
/UI-REVIEW.md
