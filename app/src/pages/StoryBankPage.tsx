import { MarkdownDocPage } from '../components/docs/MarkdownDocPage'

// Raw anecdote material for essays/secondaries (content/story-bank.md). Editable.
export function StoryBankPage() {
  return <MarkdownDocPage path="story-bank.md" intro="Story Bank — raw moments for essays & secondaries. Capture freely; the agent pulls from here." />
}
