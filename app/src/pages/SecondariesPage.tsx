// Placeholder — secondaries are the next phase. Per-school secondary essay tracking
// (status, prompts, deadlines, notes) will live here, one detail view per school.
export function SecondariesPage() {
  return (
    <div className="page page--single">
      <div className="placeholder">
        <h2 className="placeholder__title">Secondary Tracker</h2>
        <p className="placeholder__body">
          Coming next phase. This is where each school's secondary application will be tracked in
          detail — secondary essay status, prompts, deadlines, and per-school notes — separate from
          the school grid on the Application Tracker.
        </p>
      </div>
    </div>
  )
}
